# Miyouzi_AP
__超轻量批处理热点创建工具__
<br>
支持当有设备接入/断开热点是弹窗提醒（可关闭）。

支持自动查询MAC。

支持记录所有接入设备的MAC信息，并自动查询其网卡。

陌生MAC查询后将记录在 MyMacList.ini ，未来该设备再次接入将直接提取 MyMacList.ini 里的记录。

可编辑 MyMacList.ini ，更改已知MAC的信息为自己想要的备注。

支持记录设备接入/断开的历史（可关闭）。

支持设置log大小上限。

一键创建开机后台自起热点。（不影响弹窗提醒）。

配置文件是setting.ini,可打开按需设定。